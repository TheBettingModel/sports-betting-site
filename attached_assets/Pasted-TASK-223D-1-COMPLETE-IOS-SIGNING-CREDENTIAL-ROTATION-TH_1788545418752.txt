TASK #223D-1 — COMPLETE IOS SIGNING CREDENTIAL ROTATION THROUGH EXPO/EAS

We are finishing the NCAAF production deployment today.

Do not start any new features, model upgrades, UI changes, refactors, #224 work, or unrelated tasks.

This task has ONE objective:

Replace the compromised Apple Distribution Certificate and provisioning profile using Expo/EAS managed credentials, verify that the replacement works with a production iOS build, and retire the old compromised credential.

==================================================
CURRENT VERIFIED STATE
==================================================

The App Store Connect API key remediation is ALREADY COMPLETE.

Completed:

- A replacement App Store Connect API key was created.
- The replacement key is stored in Expo/EAS.
- The replacement key is named "TBM EAS Submission".
- The old compromised App Store Connect API key ending in 49S was revoked in App Store Connect.
- Do NOT recreate or modify the App Store Connect API key unless verification proves there is a problem.

The remaining Apple credential issue is the Distribution Certificate.

The current/old EAS Distribution Certificate has a serial number ending:

9E35

This is the distribution credential that needs to be replaced as part of the security remediation.

The production EAS build profile already uses:

credentialsSource: remote

Keep remote credential management enabled.

==================================================
IMPORTANT — OWNER DOES NOT MANUALLY MANAGE CERTIFICATES
==================================================

Use Expo/EAS managed credentials.

DO NOT ask the owner to:

- create a CSR
- use Keychain Access
- manually generate a private key
- manually create a .p12
- manually export a .p12
- manually create a provisioning profile
- upload private signing material into Git/Replit
- paste any private key into chat
- provide the contents of a .p8 or .p12

The owner should only be involved if Apple authentication/2FA is genuinely required.

If authentication is required, STOP at that exact point and tell the owner the ONE minimum action required.

After the owner completes authentication, continue the task.

==================================================
STEP 1 — VERIFY EXPO/EAS ENVIRONMENT
==================================================

Work from the existing TBM repository.

Determine the correct project/mobile directory from the repository rather than guessing.

Check the current EAS environment:

eas --version
eas whoami

Inspect the existing EAS configuration and confirm the production profile uses remote credentials.

Inspect the current iOS production credentials through the supported EAS credentials workflow.

Use:

eas credentials

Select the appropriate:

iOS
production build profile
Build Credentials

Do not delete working credentials blindly.

Record the CURRENT distribution certificate serial before replacement.

It should correspond to the existing EAS certificate ending:

9E35

==================================================
STEP 2 — CREATE NEW EAS-MANAGED IOS SIGNING CREDENTIALS
==================================================

Use the supported Expo/EAS managed-credentials workflow to generate a BRAND-NEW Apple Distribution Certificate.

Do not reuse the existing 9E35 certificate.

Also generate/create the matching production provisioning profile required by the new certificate.

Allow EAS/Apple to generate and manage the signing material.

If EAS requires Apple Developer authentication or 2FA:

STOP.

Tell the owner exactly what authentication action is required.

Do not give the owner CSR/Keychain/.p12 instructions.

Once authentication is completed, continue automatically.

==================================================
STEP 3 — VERIFY THE REPLACEMENT
==================================================

After EAS creates the new credentials, verify all of the following:

1. A new Distribution Certificate exists.

2. The new certificate is VALID.

3. The new certificate serial is DIFFERENT from the previous certificate ending 9E35.

4. A new production provisioning profile exists.

5. The provisioning profile is associated with the correct TBM iOS application.

6. The production EAS profile continues using remote credentials.

7. No private signing files were written into the Git repository.

8. No .p8, .p12, .cer, .mobileprovision, or credentials.json signing artifacts were added to source control.

Do not expose private credential contents in logs or the report.

==================================================
STEP 4 — TEST THE NEW SIGNING CREDENTIALS
==================================================

Once the new certificate and provisioning profile are active, perform a real production iOS build using the existing production profile:

eas build --platform ios --profile production

The purpose of this build is to prove that the new EAS-managed signing credentials work.

DO NOT submit a new App Store version as part of this task unless submission is technically required to verify signing.

A successful signed production build is the verification gate.

==================================================
STEP 5 — RETIRE THE OLD COMPROMISED CERTIFICATE
==================================================

ONLY AFTER the replacement credentials exist and the new production signing build succeeds:

Identify the old compromised Apple Distribution Certificate corresponding to the previous EAS certificate ending:

9E35

Retire/revoke ONLY that compromised certificate.

DO NOT revoke every Apple Distribution Certificate.

DO NOT revoke an unrelated certificate.

If the exact old Apple certificate cannot be positively identified automatically, STOP and give the owner ONE simple action needed to identify/revoke it.

Do not send the owner through a complicated manual certificate workflow.

Because provisioning profiles associated with a revoked distribution certificate can become invalid, ensure the NEW production provisioning profile uses the NEW certificate before the old certificate is revoked.

After revocation, verify that the replacement EAS Distribution Certificate and provisioning profile remain valid.

==================================================
STEP 6 — SECURITY CHECK
==================================================

Run the repository signing-credential security regression check:

pnpm run security:signing-credentials

Also inspect git status.

Verify that this credential rotation did NOT introduce:

- .p8 files
- .p12 files
- .cer files
- .mobileprovision files
- credentials.json
- private key material
- Apple signing secrets

into tracked source control.

Do NOT commit private signing credentials.

==================================================
DO NOT DO THESE THINGS
==================================================

Do NOT:

- change the NCAAF model
- change Baseline D
- start #224
- promote NCAAF V4
- change publication thresholds
- change NCAAF picks
- change the game-day pipeline
- work on tomorrow's games
- redesign the website
- modify other sports
- deploy unrelated changes
- submit a new App Store release unnecessarily
- manually expose signing credentials

This task is ONLY the final Apple Distribution Certificate / EAS signing remediation.

==================================================
REQUIRED FINAL REPORT
==================================================

When finished, return this exact checklist with the actual results:

TASK #223D-1 — IOS SIGNING REMEDIATION

App Store Connect replacement key in EAS: PASS / FAIL
Old App Store Connect key ending 49S revoked: PASS / FAIL

Old EAS Distribution Certificate ending 9E35 identified: PASS / FAIL
New EAS Distribution Certificate generated: PASS / FAIL
New certificate serial differs from 9E35: PASS / FAIL
New production provisioning profile generated: PASS / FAIL
EAS production credentialsSource remote: PASS / FAIL

Production iOS signing build: PASS / FAIL

Old compromised Distribution Certificate retired/revoked: PASS / FAIL

Repository signing credential scan: PASS / FAIL
Private signing files introduced into Git: YES / NO

OWNER ACTION REQUIRED: YES / NO

If OWNER ACTION REQUIRED = YES:

STOP and give me ONE action only.

If OWNER ACTION REQUIRED = NO:

State:

TASK #223D-1 COMPLETE — READY FOR TASK #223D-2

Do not begin Task #223D-2 automatically.