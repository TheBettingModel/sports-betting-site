# Rollback: 20260906_001_tbm_v4_platform

This migration is additive and backward-compatible. The safe application
rollback is to deploy the previous API and disable the candidate scheduler.

Do **not** drop these tables automatically. They contain immutable model,
evidence, forecast, and scheduler audit records. If a later owner-approved
cleanup is required, export and verify those records first, then create a
separate destructive migration with an independently verified backup.